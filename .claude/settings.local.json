{
  "permissions": {
    "allow": [
      "Bash(command -v docker docker-compose colima)",
      "Bash(docker --version)",
      "Bash(docker-compose --version)",
      "Bash(colima version *)"
    ]
  }
}
